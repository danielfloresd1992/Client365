export const metadata = {
    title: 'Sistema de bonificación',
    description: 'Cuánto vale un bono, a qué cambio se paga, y qué alertas bonifican con cuál regla y en qué establecimientos.',
};

export default function BonusLayout({ children }) {
    return <>{children}</>;
}
