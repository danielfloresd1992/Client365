export const metadata = {
    title: 'Herramienta de integraciones',
    description: 'Las llaves con las que otros sistemas —el asistente de IA, paneles, scripts— consultan la API de Jarvis365.',
};

export default function IntegracionesLayout({ children }) {
    return <>{children}</>;
}
