import '../../style/style.css';

export const metadata = {
    title: 'Corte365',
    description: 'Corte por hora - Rotaciones, procesos y toques',
};

export default function CorteLayout({ children }) {
    return <>{children}</>;
}
