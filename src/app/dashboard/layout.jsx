export const metadata = {
    title: 'Panel analítico',
    description: 'Panel analítico en tiempo real de Jarvis365: alertas del día, locales en horario y personal de operaciones.',
};

export default function DashboardLayout({ children }) {
    return (
        <>{children}</>
    );
}
